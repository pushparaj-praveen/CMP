# College Management Portal

Frontend project for the College Management Portal.

## Run Locally

**Prerequisite:** Node.js

1. Install dependencies:
   `npm install`
2. Start the development server:
   `npm run dev`

The app runs with Vite and opens on the configured local development port.
